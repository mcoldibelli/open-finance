server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      enabled: false
      server:
        native:
          search-locations: classpath:/config-repo
  profiles:
    active: native

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    health:
      show-details: always