spring:
  application:
    name: gateway-service
  config:
    import: "configserver:http://localhost:8888"
  profiles:
    active: local
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 3
        initial-interval: 1000